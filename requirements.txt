# Options Trader — Direct Python Dependencies
#
# These are the packages this project imports directly, pinned to the exact
# versions the test suite passes against.  Transitive dependencies are
# resolved from this file and recorded in requirements.lock.
#
# Local install:      pip install -r requirements.txt
# Container build:    uses requirements.lock (exact transitive closure)
#
# To change a dependency: edit the pin here, then regenerate the lock:
#   docker run --rm -v "$PWD:/w" -w /w python:3.13-slim sh -c \
#     'pip install -q -r requirements.txt && pip freeze' > requirements.lock

# Robinhood API
robin-stocks==3.4.0

# TOTP authentication
pyotp==2.9.0

# Options data + live prices
yfinance==1.2.0

# Black-Scholes delta calculation (POP-weighted spread scoring)
scipy==1.17.1

# Earnings calendar + NYSE holidays
finnhub-python==2.4.27
exchange-calendars==4.13.2

# Resend email delivery
resend==2.30.1

# Email HTML templating (optional — falls back to inline renderer if absent)
Jinja2==3.1.6

# Spreadsheet reading
pandas==3.0.1
openpyxl==3.1.5

# HTTP requests (setup wizard validation)
requests==2.32.5

# Config + secrets
python-dotenv==1.1.0
PyYAML==6.0.3

# Scheduler
schedule==1.2.2

# Timezone handling (Python 3.9+ stdlib zoneinfo covers this,
# but tzdata is needed on systems without IANA timezone data)
tzdata==2025.3

# Web API (container --serve mode)
fastapi==0.115.6
uvicorn[standard]==0.34.0
